#!/usr/bin/env bash
# Versioned pre-commit hook: the anti-leak guard over staged files (method/conventions.md §9/§10/§11).
# Enable once per clone:  git config core.hooksPath tools/git-hooks
exec "$(git rev-parse --show-toplevel)/tools/check-leaks.sh"
