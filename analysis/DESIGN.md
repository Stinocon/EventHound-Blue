---
title: Analysis and correlation engine — design and plan
updated: 2026-08-29
version: 0.35.0
state: "see docs/roadmap.md — the single source of truth for what is built and what is open. Three hand-maintained inventories said the same thing and drifted; this field is now a pointer, not a fourth. (Historical value at 0.22.2: Phases 1-2 implemented, Phase 3 landed, host-artifact spokes remaining.)"
related_files:
  - method/conventions.md
  - rag/README.md
  - method/anonymization.md
  - method/framework/INDEX.md
  - analysis/README.md
  - analysis/schema/common-schema.md
  - docs/analysis/threat-hunting-evtx.md
  - docs/analysis/performance.md
changelog:
  - "0.35.0 — 2026-08-29 — both images built and exercised, not inferred. In `rag-api` the shared `hostmem` imports and the guard refuses; in `eventhound` /api/config is a 200, the scoring oracle answers from inside the container, and a model chosen in the GUI reaches `configured_model()` through the named volume. The baked Linux binaries execute and the host's macOS ones did not leak in with the ignore-file move."
  - "0.34.0 — 2026-08-29 — the eventhound image can see `tools/`. It built from `./analysis`, which carries none, so in Docker the Settings view was a 500 and the assistant could not import the scoring oracle or the enrichment gate — §6's deterministic numbers absent from one of the three declared runtimes. Root build context, `COPY tools/ /tools` (where the code already looks), `analysis/.dockerignore` folded into the root allowlist since Docker reads only the one at the context root, and a named volume for the settings store so keys typed into the GUI survive the container without bind-mounting real `data/` (§10). Also closed: the reindex window between the delete and an ingest that can now refuse, and the inability to tell an unindexed collection from an empty result set."
  - "0.33.0 — 2026-08-29 — R6 on the §14.4 kill-chain digest. §9 verified clean on every production path. The rest: a bare `[:220]` per line fabricated evidence rather than shortening it (a truncated domain and a neighbouring real IP offered as observed, a technique ID clipped into a different valid ID), headers printed totals beside four visible rows, the block displaced the unified timeline out of the budget, the window width was dropped, the GUI's kill-chain button bypassed the packer entirely and lost the co-occurrence label, and `run_toolbench` packed before redacting — an order §14.5 itself described. All corrected; §14.5 now states redact-then-pack, which is what the two production callers always did."
  - "0.32.0 — 2026-08-29 — R6 on the RAG guard slice. The root `.dockerignore` allowlist added a day earlier did not allowlist: Docker re-tests patterns against parent prefixes and applies that to `!` patterns, so `!rag`/`!tools` re-included their whole subtrees — 16,147 files and 5.97 GB into a context meant to hold 19 (data/ excluded throughout, §10 intact). Deleted; they were never needed. The constructor-guard claim in §14.9 is narrowed to what is true: the dense model is constructor-guarded, the reranker is still guarded at its one lazy getter, BM25 is 88 KB and unguarded on purpose. Also: `check.sh --rag` now distinguishes a memory refusal from a coverage failure, and the Dockerfile no longer claims its own reading is the machine that runs out on Docker Desktop for macOS."
  - "0.31.0 — 2026-08-29 — R6 on 0.29.0's model work, twelve findings, the six that mattered fixed. The installer's `model_ok` was tag-blind, so the model the picker had just made selectable was reported present and never pulled; `POST /api/ai/model` stored any string, including one that broke out of the option's `value=` attribute (`aiEscape` escapes `<>&` and not `\"`); `_remedy()` answered a refused 1.2 GiB load by recommending a 5.0 GiB one, because its `!= DEFAULT_MODEL` test predates the default becoming the smallest model; the endpoint's verdict cost three probes, the first at the 180 s chat timeout; it returned a bare 500 where `tools/` is absent; and the GUI kept pinning the model per request, so the persistent setting was one-directional. §14.3's precedence paragraph stands — the review re-implemented `70483b8`'s broken lookup and confirmed the new regression test fails against it."
  - "0.30.0 — 2026-08-29 — R6 round three, and the regression it found in round two's own fix. `kc_phase IS NULL` had been replaced by `coalesce(techniques,'') = ''` as the test for \"carries no ATT&CK technique\". Both are proxies and each misses what the other catches: 345 of the 3142 vendored SigmaHQ rules declare a TACTIC and no technique ID, so `store._attack_of` resolves their phase while the column stays NULL — verified by execution, the substitution deleted a host whose only detection was tactic-only from the triage list entirely, moved a mixed host's `first_seen` three hours later, and quoted a phase's own establishing detection as another phase's corroboration, which is the one sentence that view promises never to say. Both spellings, being raw-string tests, also counted a YARA `attack = \"Credential Access\"` meta value as a technique. After three wrong spellings the answer is not a fourth: the store gained a derived `attack_evidence` column (a resolved phase, or a well-formed T#### id via `attack.split_techniques`), and `phase_corroboration`, `host_killchain`, `incident_clusters`, `recipes.technique_frequency` and the timeline's `why` all read it. Also from the same round: the HTML report and the GUI card returned empty whenever `killchain` was empty, hiding the per-host table for exactly the host with unmappable evidence; `incident_clusters` still rendered the ambiguous empty depth; the `coalesce(max(kc_order),0)` added the day before was dead code with a false comment (`kc_order` is 0, never NULL). Tests now pin all four inputs — tactic-only, unresolvable ID, free text, and no evidence at all."
  - "0.29.0 — 2026-08-29 — the default model follows the corpus, and the picker that chooses otherwise finally works. `default_model()` returns `qwen2.5:7b-instruct` on every host: the RAM branch added on 2026-08-28 answered the hardware question and left the quality one open, and the toolbench corpus of 2026-07-24 had already answered that one against the 14b (54/60 vs 44/60, zero non-Latin-script answers vs 7 of 60, 2.2x faster, ahead only on narration by one event in 42). The 14b stays selectable, and selecting it is now a real thing to do: the Assistant's model picker was a per-request override that vanished on reload while `llm_model` — the setting the CLI reads — was written by nothing and, since `70483b8`, read through the wrong key (`load().get(...)` against a store that keeps settings under `data[\"settings\"]`). One control now does both, through `get_setting`, and `POST /api/ai/model` answers with this host's verdict at the moment of choice rather than three seconds into a streamed answer. `_configured_model` became public `configured_model()` because the installer and the uninstaller were asking `default_model()` and could not see a model the analyst had chosen. Tests: the stored-choice round trip in tests/test_ai.py and the GUI-to-engine one in test_gui."
  - "0.28.0 — 2026-08-29 — the three fixable defects the second round left open, and the two the class hunt behind them turned up. The RAG memory guard moved from `retrieve`'s lazy getters into `Embedder`'s constructor — a guard on the callers has to be re-applied by every new caller, and `evaluate`/`ingest` had never applied it — and the `rag-api` image is now built from the repository root so `tools/hostmem.py` exists inside it, with a root `.dockerignore` allowlist that keeps `data/` and the multi-gigabyte indexes out of the context. `ai/context` gained a kill-chain renderer (§14.4): the phases, the per-host depth, the technique catalogue and the corroboration, labelled as co-occurrence — the assistant was the fifth renderer and the only one blind to it. `kc_phase IS NULL` stopped standing in for 'carries no technique': the phase is derived through the vendored ATT&CK map, so an unresolvable ID left real detection evidence to be rendered as a tool that merely co-occurred; the predicate now reads the `techniques` column. The class hunt then found the same proxy in `host_killchain`, where it was worse — a host whose only detection carried an unresolvable ID was missing from the triage list entirely — and, unfixed, that the `eventhound` image cannot see `tools/` either, which is a live break of the GUI settings and the assistant's scoring/enrichment tools in Docker (docs/roadmap.md 1.27.0)."
  - "0.27.0 — 2026-08-28 — the second adversarial round, and the four defects the FIRST round's fixes created. The shared-module import was fatal in Docker and crash-looped the GUI container; the swap gate killed the RAG on the reference machine on a premise that machine falsifies (macOS grows the swapfile, so the ratio never falls) and was never load-bearing for the incident anyway — swap is now context inside a refusal, never a verdict; the reserve gained a 4 GiB ceiling; residency requires an exact tag; a failed /api/ps is unknown rather than absent; and the per-call sys.path insert fixed in one file was still live in its sibling. Full list, including what was left open, in docs/roadmap.md 1.26.0."
  - "0.26.0 — 2026-08-28 — the guard reaches the project's OTHER gigabyte-scale allocator. `memory_preflight` covered the on-box LLM and nothing else, while the RAG loads its dense, sparse and reranking models lazily on the first query and keeps them resident — on the same host, checking nothing. Rejected: a mutual exclusion between rag-api and Ollama. It would be a static pairwise rule competing with a dynamic measurement that is strictly better informed (the preflight reads real free memory, so a resident rag-api is already in the count), and enforcing it would mean either stopping services behind the operator or refusing to start ones that coexist fine on a large host. Done instead: `hostmem` moves to `tools/` (two callers now, and duplicating the policy would let the two allocators hold different opinions about one machine) and grows `pressure_reason()` — the weaker question, asked because fastembed reports no footprint and a size here cannot be honestly measured: is there any headroom at all. `retrieve._guard_memory` calls it at the three lazy loaders, fails open when the shared module is unreachable or nothing can be measured, and shares the EVENTHOUND_ALLOW_LOW_MEMORY switch so an operator learns one and not two. Offline test in pipeline/test_rank_boost.py."
  - "0.25.0 — 2026-08-28 — the memory guard, corrected by the adversarial round that followed it. Seven defects, several defeating it: `setup.sh doctor` rendered \"could not measure\" as a green \"host can load <model>\", so a fresh install — model not pulled, therefore no measurable size — asserted the opposite of the truth on the machine that had lost its session (new three-valued `memory_status()` keeps ok/unknown/refuse apart). `available_bytes()` double-counted `Pages purgeable`, a cross-cutting label over pages already in active/inactive, and remains optimistic besides — answered with an explicit `reserve_bytes()` (15% of RAM, floor 1 GiB) rather than a fudged discount. `_model_bytes()` cached a failed measurement, so a client built before the pull kept the guard dead for its whole life. An untagged model name sized against whichever tag Ollama listed first (2x spread); ambiguity now resolves upward. `MemAvailable: 0` was falsy and read as \"cannot measure\", inverting the one reading that says there is no memory. `docker-compose.yml` pinned EVENTHOUND_LLM_MODEL to the 9 GiB literal, and the environment wins over `default_model()`, so every container deployment defeated the fix. And the guard ran up to three 180 s HTTP probes synchronously on the GUI event loop — now one shared /api/tags fetch at a 5 s probe timeout. Tests pin each one."
  - "0.24.0 — 2026-08-28 — THE MODEL NOW HAS TO FIT THE HOST. On 2026-08-28 the suite took a 16 GB Mac's graphical session down: Ollama loaded qwen2.5:14b (9.1 GiB predicted) beside the RAG stack, Qdrant, the GUI and DuckDB, with swap already at 92%. Ollama logged the problem itself (`system_limited=true`) and loaded anyway; nothing on our side was looking. §14.9 had named this risk ('Hardware floor — document a minimum') and left it as documentation. Three changes. (1) `ai/hostmem.py`: RAM, available memory and swap pressure, stdlib-only, per-platform, every reading None-able. (2) `ollama_client.default_model()` — the 14b below PREFERRED_MODEL_MIN_RAM_GIB (24) falls back to 7b-instruct, so the installer no longer pulls 9 GB a 16 GB host cannot load; the shell asks Python for it (setup-common.sh, uninstall.sh) rather than repeating the literal. (3) `memory_preflight()` enforced inside chat/chat_stream — the two calls that make Ollama allocate — refusing a load that would not fit (available < size×1.15, or swap >85%), skipped for an already-resident model, failing OPEN on every unmeasurable input, overridable with EVENTHOUND_ALLOW_LOW_MEMORY=1. New `unusable_reason()` unifies 'not pulled' and 'will not fit' for the four gates (CLI, GUI, toolbench, e2e test) that had all been asking the weaker question. `doctor` reports the verdict. tests/test_ai.py `run_memory_guard` pins it against injected host readings. NB (§14.9): the toolbench corpus already argued against the 14b default on quality grounds and was not acted on."
  - "0.23.0 — 2026-08-27 — corrected four claims this document made about its own product. §12 (threat-intel enrichment) said 'roadmap, not yet implemented' while tools/enrichment/ is complete and golden-tested; §14 (on-box LLM) said 'design only. Nothing here is built yet' while analysis/ai/ ships five modules, a CLI and three endpoints; the Phase 0 table still said the Zircolite-vs-Hayabusa choice was 'to be decided in Phase 1' although Phase 1 closed on 2026-06-16 and nothing ever referenced Zircolite; and the API-key location named a directory (analysis/.secrets/) that was never created — the store is data/config.json via tools/eventhound_config.py. Added the attack map and the always-on case to the state line."
  - "0.22.2 — 2026-08-27 — Phase 3 corrected from SUSPENDED to DONE (the adapter landed 2026-07-24; the entry had never been updated) and the pointer to the removed docs/crowdstrike/ dropped. Vendor product documentation left the RAG on 2026-08-27 — cs_falcon_docs and sonicwall_docs deleted, CrowdStrike/SonicWall moved to a separate project on their official MCP servers. Only the documentation half is affected: the ingest adapters are unchanged."
  - "0.22.1 — 2026-07-24 — A DROPPED EVTX IS NOW REPORTED, not silently absent. Hayabusa exits 0 on a file it cannot read: it skips it, records it in a per-run `logs/errorlog-<ts>.log`, and prints a pointer to that log — so nothing in the exit status or output tells a caller a file vanished, and a corrupt or truncated EVTX in a client collection just made the timeline shorter with no reason to doubt it. `hayabusa_runner.failed_files(stdout)` parses the pointer and reads the log; `run` and `run_batch` take a `skipped` out-parameter and fill it; `build_records` collects both paths' skips (the batch path maps the staged name back to the caller's own, since duplicate-basename staging renamed it) and emits one `errors` entry each — 'evtx X: skipped by Hayabusa (unreadable or corrupt)'. Collected once after the EVTX block rather than raised inline, because raising inside _batch_evtx would trip its own per-file fallback and re-run everything, and raising in a worker thread would lose the message. That `errors` list was already surfaced everywhere — CLI (run_analytics prints each), report_html banner, GUI warnings, bundle meta — so no plumbing was added downstream. Basenames only (§9: an error string reaches report, GUI and bundle, and a full path can carry a case or host name). failed_files is best-effort: an unreadable or dangling log yields [], because failing an ingest over its own error reporting would be worse than the silence it replaces. Tests (test_hayabusa_toolbox): failed_files on a fabricated stdout+log (no binary needed) + both directions of the dangling/clean cases; e2e asserts an unreadable EVTX among readable ones is reported through build_records on both the batch and single-file paths, and costs the readable one nothing. Closes the item 0.22.0 left open."
  - "0.22.0 — 2026-07-24 — FOUR OPEN ITEMS, AND THE REVIEW THAT FOLLOWED THEM. Landed: EVTX BATCH INGEST (`hayabusa_runner.run_batch` directory mode from 2 files up, `runner.stage_batch_dir` symlink staging with numeric prefixes for the duplicate basenames a real collection is full of, per-file ThreadPoolExecutor kept as fallback) — 6 files measured at 2.8 s against 17.5 s per-file, identical record counts; OSQUERY (`adapters/osquery_result.py`, NDJSON result log, nested columns{} and epoch unixTime, table→ECS category map, `engine/run_osquery.py`, GUI content-sniff); CROWDSTRIKE (`adapters/crowdstrike.py`, detection clipboard Key:Value + LogScale/CQL JSON incl. @rawstring wrapper, `engine/run_crowdstrike.py`, GUI content-sniff); YARA (`engine/run_yara.py`, `build_records(yara=…)`, `/api/yara-scan` — endpoint only, no view yet). WHAT THE REVIEW FOUND, all of it invisible to a green gate. (1) `build_records(crowdstrike=…)` raised AttributeError on EVERY call: the parameter shadowed the bare `crowdstrike` module import, and the exception was swallowed into `errors`, so the CLI and the GUI returned an empty analysis with a one-line note — the adapter had never once run through the path the product uses, because all three tests called it directly. Module aliased, and a test now goes through build_records. (2) `aip` — the agent's EXTERNAL address, i.e. the tenant's NAT egress, the same value for every host behind it — was mapped to `source.ip`: as a correlation indicator it bridges every CrowdStrike event to every other one, the noise loopback and SYSTEM are excluded to prevent. Namespaced only. (3) osquery's `columns.category` (the FIM group, 'homes') overwrote the table-derived ECS `event.category`; `tty` was mapped to a port; `name` was read as a process name in tables where it names a kernel extension. (4) §9: the fixtures carried a real customer ID, agent ID, hostname, a user surname inside a file path and the tenant public IP verbatim into a versioned test — the pseudonym map was still the empty template, so `check-leaks.sh` had nothing to match and passed. Pseudonymised; map populated. (5) The batch-staging test re-implemented the collision rule instead of calling it — green whatever the code does. (6) LogScale events were all labelled `event.category: process`, whatever they were. MEASURED, NOT ASSUMED: the error contract batching was said to preserve was already weaker than the roadmap claimed — Hayabusa skips a file it cannot parse and exits 0, so per-file reported nothing either; a corrupt EVTX is silently absent in both modes, and telling the analyst stays open."
  - "0.21.2 — 2026-07-24 — NUMBERS WE DID NOT HAVE (roadmap 'Next up' #5). Two commands replace two impressions. `engine/run_bench.py`: per-stage profile over deterministic synthetic records (three sources sharing entity pools, so correlation measures real joins rather than empty ones) plus a real Hayabusa ingest axis that skips cleanly without the binary. `engine/run_toolbench.py` + `eval/toolcall_prompts.json`: 20 labelled prompts, 14 requiring a named tool, scoring emit / correct-tool / argument validity / cap / latency and — the metric the §14.3 model switch was made on — NARRATION, the required call absent while the answer talks as though it happened. THREE DEFECTS THE MEASURING FOUND. (1) The store load *is* the pipeline: every recipe and correlation together are under 1% of a run, because DuckDB's Python client probes `import pandas` per bound value and Python does not cache a failed import — 35 652 failed sys.path walks per 1000 records. `store._without_probing_for_pandas` negative-caches the name for the insert and restores it after: 3.8x on the dominant stage (2000 records 5.21s -> 1.37s), 100k events now load in 77s instead of ~4.5 min. (2) EVTX ingest is priced per FILE, not per megabyte: Hayabusa is invoked once per file and reloads ~5000 Sigma rules each time (~540 MB resident per invocation, run cpu_count() at a time). The same 278-file corpus takes 205 s that way and 7.5 s handed to Hayabusa as a directory — 27x — NOT applied here because directory mode gives no per-file error report and build_records isolates failures on purpose; it is on the roadmap with the number attached. (3) The 5000-record cap was never a performance limit but a silent one: it bounds only the browser-side IoC corpus and the THOR list (SQL analytics always saw everything), yet a miss in that search was indistinguishable from a real absence — analyze() now reports `records_total`/`records_capped` and the GUI states its scope (`lib.iocScopeText`, unit-tested). Benchmarks stay out of the default gate (`tools/check.sh --bench` runs the quick profile): they report, they do not assert, since a timing threshold would encode the speed of the machine that wrote it. Figures + hardware in docs/analysis/performance.md. No product behaviour change beyond the store speed-up and the cap disclosure — APP_VERSION stays 0.21.0."
  - "0.21.1 — 2026-07-23 — TESTS THAT MEASURE (roadmap 'Next up' #4). Three defects in how the suite reported itself. (1) SKIPS WERE PASSES: a missing binary or dataset made a test print SKIP and return 0, which pytest reads as success — MFT end-to-end, registry hives and YARA were green for code that never ran. `tests/_helpers.skip_test` raises a real pytest skip while still printing and returning 0 as a script, so both execution modes stay honest; 18 sites converted across 10 files (one deliberately not: a per-case `continue` inside a loop in test_evtx_slice, where a skip would abandon the rest). (2) COVERAGE WAS UNKNOWN: pytest+pytest-cov added as a dev-only extra (the product still runs on duckdb alone; check.sh falls back to script mode without them). First measurement 62%, and it named the hole — every `engine/run_*.py` at 0%, i.e. the CLI, the surface the project calls primary, never executed by a test. `tests/test_cli_smoke.py` discovers entry points by glob (a new CLI is covered the day it is written), asserts all 13 start, and runs the offline ones end to end (decode, case lifecycle, export->report bundle round trip, corpus). Subprocess coverage had to be turned on (`parallel=true` + COVERAGE_PROCESS_START via tests/_helpers.subprocess_env), otherwise the number kept reporting 0% for code it was demonstrably running — a coverage figure that lies is worse than none. Suite now 71%. (3) THE JS WAS SYNTAX-ONLY: pure logic extracted from the 2600-line inline script into `gui/static/lib.js` (loaded as a classic script before the page, plus a CJS export tail for Node) and unit-tested with `node --test` — no new dependency. check.sh collapses nine hand-listed analysis groups into one pytest invocation that prints the coverage number, and gains a JS test check."
  - "0.21.0 — 2026-07-23 — CASE PERSISTENCE (roadmap 'Next up' #3). DuckDB was in-memory, an analysis lived for the length of a request, and a bundle was a snapshot of the conclusions — so reopening, annotating and comparing two analyses of the same host were impossible. `analytics/case_store.py`: one directory per case (`analysis/cases/<id>/`) with a file-backed DuckDB and a JSON of metadata + notes. TWO TABLES on purpose — `events` (the analytic table) and `raw_records` (the original common-schema dicts): the second lets a case outlive a schema change, because `connect()` compares a fingerprint of the column layout (`store.schema_fingerprint`) and rebuilds `events` from the raw records instead of querying columns that no longer exist; without it every store.py change would rot every stored case. `store.py` split into `create_events_table` + `insert_records` (ts_parsed now part of the DDL, ids continue across appends) so sources are added incrementally — peak memory is one source, not the whole case; the parsing step upstream still materializes its own file, so this lifts the analytics ceiling, not the adapters'. `runner.analyze_con(con, records)` extracted from `analyze()`, plus `analyze_case()`. CLI `engine/run_case.py` (list/new/add/show/analyze/note/diff/rm; rm is destructive → typed confirmation, refuses without --yes when stdin is not a TTY, §12); `diff` finally makes baseline/diffing serious, since both sides now survive their session. GUI: Cases view + endpoints, and `/api/analyze` accepts a `case` field that persists the FULL record set server-side (no browser round-trip, no 5000 cap). Case ids arriving from a browser are validated against a strict pattern (§8) and deletion is bounded to the case root. `tests/test_case_store.py` asserts the equivalence that justifies the module — a persisted analysis returns exactly what the in-memory one returns — and writing it EXPOSED A LATENT BUG: `string_agg` without ORDER BY made `source_list`/`variants`/`realms`/phase and tactic blobs depend on row order, so two runs of the same data could differ; all now ordered, output is deterministic. §9/§10: `analysis/cases/` gitignored and added to the leak guard's forbidden paths."
  - "0.20.1 — 2026-07-23 — CORRELATION EVALUATION CORPUS (roadmap 'Next up' #2). The confidence weights, the beaconing jitter threshold and the episode session-gap were hand-set and never measured; the RAG had golden queries, correlation had nothing. `eval/correlation_corpus.json` (10 labelled cases, synthetic + offline, RFC 5737/3849 addresses and pseudonyms only) declares records plus what the engine MUST conclude — bridges, entities that must NOT bridge, confidence ordering, multi-family episodes, cluster membership, beaconing present/absent, timeline selection — and every case names the knob it guards and the security claim (`rationale`) behind the label. `engine/run_eval.py` runs it (`--case`, `-v`, `--sweep-gap`). MEASURED RESULT: the session-gap holds fully only between 120 s and 300 s — below, a real chain corroborated by a shared IP splits; above, two unrelated sources eight minutes apart merge — so the 120 s default sits at the lower edge of a measured band instead of being a preference. `tests/test_corpus_discriminates.py` breaks one knob at a time and asserts the guarding case turns red (7 mutations, all caught): a corpus that cannot fail measures nothing. Both wired into check.sh. No product behaviour change — APP_VERSION stays 0.20.0."
  - "0.20.0 — 2026-07-23 — UNIFIED TIMELINE (roadmap 'Next up' #1): `correlate.timeline` existed but nothing called it, and a chronological cap over everything would have returned the head of the file (boot noise) rather than the incident — so it is now salience-filtered: a row is kept when it carries an ATT&CK technique, a high/critical Sigma hit, a **high-signal event type** (`_NOTABLE_ACTIONS`, read from the Event ID SOT vocabulary: log clearing, audit-policy change, service/scheduled-task/WMI persistence, privileged group change, remote thread, failed auth — process-create/image-load/scriptblock deliberately excluded as flooders), a THOR finding or a failed action, and each row states which criterion earned it its place (`why`). The action criterion is what makes a full EVTX stream with no rule hits still useful: on a real sample, 21 log-clearing events (EID 104) were kept on their own merit rather than only by the empty-set fallback. `why` is a SELECTION criterion, not a severity and not a ranking (§6); the axis stays strictly temporal. Empty-notable datasets fall back to the chronological head labelled `context`, so the view never reads as 'nothing happened' when it means 'nothing matched'. Wired into `analyze()` (`timeline`, capped at 500), the HTML and Markdown reports, the GUI (Timeline view, client-side filters over the loaded rows — no extra round trip with client data), and the AI context packer (as an ordered sequence, not one more row list: the order is the point). tests/test_correlation.py extended (notable selection, filters, context fallback). Also corrected docs/analysis/correlation.md, which still framed correlation as two primitives."
  - "0.19.0 — 2026-07-22 — Analysis bundles (recorded retroactively — the shipped version had no DESIGN entry): `engine/bundle.py` ({bundle_version, created_at, tool_version, meta, analysis}, evidence deliberately excluded §9), `engine/run_export.py`, `run_report.py --from-bundle/--format/--level`, GUI export via /api/report (format=bundle) + client-side import; `engine/version.py` becomes the version SOT. tests/test_bundle.py + a GUI round-trip assertion."
  - "0.18.0 — 2026-07-22 — Correlation, two upgrades on top of normalization: (B) CONFIDENCE ranking of shared_indicators — a strength heuristic by kind (hash 1.0 > user 0.85 > host 0.65 > domain > file > ip 0.4, boosted by #families, softened when normalized), so solid bridges (a shared hash) sort above noisy ones (a shared proxy IP); `confidence`/`confidence_label` fields (NOT an oracle security score, §6). (C) INCIDENT CLUSTERS — `correlate.incident_clusters` Union-Find over entities co-occurring in the same event → connected components; a cluster spanning ≥2 source families is one incident reaching across tools (Okta login → EVTX host → THOR hash link up as users/hosts/hashes in one cluster). Surfaced: report gains a clusters section + confidence column; dashboard card shows cluster pills + confidence/normalized badges. tests/test_incident.py (3). analyze() returns incident_clusters."
  - "0.17.0 — 2026-07-22 — Correlation: ENTITY NORMALIZATION so cross-tool joins work when tools spell an entity differently. `analytics/normalize.py` (canon_user: strip UPN/NetBIOS → account, lowercase; canon_host: FQDN→short, IP-safe; is_generic_user: exclude ubiquitous SYSTEM/service accounts from identity bridges). Computed as derived store columns (user_canon/host_canon/user_generic, Python at load — no UDF/numpy dep); correlate.py joins user/host on the canonical key and carries the raw `variants` + a `match_type` (exact|normalized) for auditability (§6). e.g. Okta alice@corp.example ↔ EVTX CORP\\alice ↔ log alice now bridge as 'alice'; DC1 ↔ dc1.corp.example as 'dc1'. Surfaced in dashboard card + HTML report (normalized badge + spellings). Also FIXED a latent bug: shared_indicators/episodes aliased the distinct-source count `sources`, but the dashboard/report expected `families` → correlation highlights were silently empty; now both aliases emitted. tests/test_normalize.py (6). schema v0.4.2."
  - "0.16.0 — 2026-07-22 — New source (DESIGN Phase 5, identity): Okta System Log. `adapters/okta_systemlog.py` parses a LogEvent export (JSON array / single object / NDJSON) → schema (event.action/category/outcome, user.name from actor.alternateId, source.ip from client.ipAddress, message) + namespaced okta.* extras; grounded on the documented LogEvent schema (validate on a real export when available). `engine/run_okta.py` CLI, `runner.build_records(okta=[...])`. Correlates by user/IP with EVTX/PCAP/logs. tests/test_okta.py (4, synthetic doc-range fixture) in check.sh; schema v0.4.1. **CLI-first**: GUI view + content-sniff routing pending (APP_VERSION stays 0.15.0 until the Okta GUI ships). M365/Entra + Proofpoint remain."
  - "0.15.0 — 2026-07-22 — New source: THOR / THOR Lite (Nextron) scan reports. `adapters/thor_scan.py` parses the .txt (key-value on ' KEY: ' boundaries; scored Warning/Notice findings) → schema (file.name/file.hash SHA256 + file.hash.md5/host/user/rule.title/ioc.*), md5s .csv fallback. `engine/run_thor.py` CLI, `runner.build_records(thor=[...])`, `thor_findings` in analyze(). Cross-source correlation on BOTH hashes: new store column `file.hash.md5` + a dedicated union in correlate.py, so a THOR hash matches EVTX/MFT whether they carry MD5 or SHA256. GUI: THOR view + content-sniff routing (.txt/.csv collide with the log adapter) + score-sorted findings table; HTML report THOR section. Tests: tests/test_thor.py (5, synthetic anonymized fixture) in check.sh. Schema v0.4.0."
  - "0.14.7 — 2026-07-21 — §14.8 refinements: (a) token streaming — ollama_client.chat_stream (NDJSON) + engine.run(stream=True) emitting token events via a _turn helper (loop unchanged); GUI streams tokens live into the bubble (replaced by the rendered final on complete), CLI run_ai --stream; (b) model picker — GET /api/ai/models + GUI dropdown, selection via the model field the /api/ai/* endpoints already accept. Tests: streaming path with a fake streaming client + models endpoint. Gate green."
  - "0.14.6 — 2026-07-21 — §14.9 default model → qwen2.5:14b (evidence-based, §14.3): in 2 real multi-tool queries the 14b reliably EMITTED tool calls (cvss_score+eid_lookup; query_analysis+eid_lookup) where the 7B narrated them in prose; correctness prevails over latency (§6). 7b-instruct kept as lighter fallback via EVENTHOUND_LLM_MODEL. Updated ollama_client default + docker-compose. Also: AI buttons extended to the Hayabusa toolbox panel (aiBar in hbRenderTable/hbAutoTable) and the Report view (explain the full analysis) via the reusable aiButton pattern."
  - "0.14.5 — 2026-07-21 — §14.8 steps 4-5 IMPLEMENTED: GUI surfaces. POST /api/ai/chat + /api/ai/explain (SSE) as a thin _ai_stream over ai.engine.run (queue+executor like _analyze_stream; start/tool/complete events; server-side anonymization gate §9; optional analysis context packed + query_analysis wired). Frontend: an Assistant chat view (sidebar item + assistant icon, #ai-messages/#ai-input, 'use current analysis' checkbox, redaction notice, reusable aiStream POST-SSE consumer, SAFE escape+fenced-code rendering of untrusted output) and per-function AI buttons (aiExplain/aiButton on dashboard overview + correlation). test_gui extended (AI endpoints, pass-or-skip on model availability); existing contract preserved; JS node --check clean. The on-box AI engine (steps 1-5) is now usable via CLI and GUI. Remaining §14.9 limit: 7B tool-call reliability (mitigate via qwen2.5:14b)."
  - "0.14.4 — 2026-07-21 — §14.8 step 3 IMPLEMENTED: anonymization gate + scoring/enrichment tools. New ai/redact.py (map-based pseudonymization from data/pseudonym-map.md, same SOT/filtering as check-leaks; case-insensitive longest-first, recursive over context+records so the query_analysis store stays pseudonymized; empty map → inactive no-op + explicit §9 warning). Wired into engine.run(redactor=) (question+context+tool results) and run_ai (redacts analysis at load; --enrich opt-in egress). Registry + cvss_score/risk_score (deterministic oracle §6) and enrich_indicator (egress-gated, public-indicators-only, private/client refused §9). Tests incl. the §14.5 property: a real identifier never reaches the model in plaintext. Steps 4-5 (GUI) remain."
  - "0.14.3 — 2026-07-21 — §14.8 step 2 IMPLEMENTED: analysis context + on-demand detail. New ai/context.py (load_analysis + pack_analysis: bounded digest — summary + top-N per recipe + correlation highlights, budget-capped, raw records never dumped). Two read-only tools: query_analysis (parameterized ops over a DuckDB store rebuilt from saved records — injection-safe, gated on a loaded context) and eid_lookup (Windows Event ID → channel-aware SOT windows_eventid; unmapped→verify — the concrete §14.9 EID-confabulation fix). run_ai --context wires it; engine.run(context=) injects the digest as data-to-interpret. Tests extended; e2e-validated on a synthetic analysis. Cosmetic: --show-calls summary made tool-agnostic."
  - "0.14.2 — 2026-07-21 — §14.8 step 1 IMPLEMENTED: on-box AI RAG-only skeleton. New package analysis/ai/ (ollama_client thin HTTP client; tools.py read-only registry with rag_search reusing the rag-api→RAG-venv fallback; engine.py owned ReAct loop + grounding/propose-not-execute system prompt §14.6) + CLI engine/run_ai.py + offline test tests/test_ai.py (fake-client loop coverage + e2e auto-skip), wired into check.sh (green). Fixed OllamaClient.available() to require an exact model-tag match (base-name match falsely reported available then 404'd on chat). No analysis context / scoring / redaction yet (steps 2-3)."
  - "0.14.1 — 2026-07-21 — §14.7 revised: Ollama is its own compose service (consistent with the containerized stack qdrant/rag-api/eventhound), not a reused host daemon. Added the `ollama` service to docker-compose.yml (loopback 11434, gitignored ./ollama_storage model volume, model pulled not baked, commented NVIDIA deploy block for Linux; macOS Docker is CPU-only); eventhound reaches it via OLLAMA_URL. No engine code yet."
  - "0.14.0 — 2026-07-21 — §14 added: design of the local conversational AI engine (on-box Ollama LLM, the product's third surface). Design only, no code (§6/§7). Decisions resolved: CLI-first module analysis/ai/ with GUI chat + per-function 'AI button' as thin layers; Ollama native tool-calling in a small owned orchestration loop; read-only tool registry (rag_search/scoring/enrichment/query_analysis — proposes, never executes §12); context packer for the small-window budget; anonymization gate before any prompt (§9); grounding+citation system-prompt contract (§6); reuse the Ollama daemon (no new compose service yet). Incremental plan §14.8, risks §14.9."
  - "0.13.0 — 2026-07-21 — doc-sync: §13 updated to reflect implemented state — Registry (RECmd + native .reg parser) and MFT (MFTECmd) adapters moved out of 'future spokes' (§13.4, new subsection); Hayabusa toolbox documented (§13.6: eid/log/computer-metrics, search, pivot-keywords, extract-base64, GUI endpoints, threat-hunting-evtx.md playbook); containerized packaging documented (§13.7: docker-compose.yml, single eventhound image with tools baked in); custom Sigma rule count 2→3 (ldap_recon_directory_service_1644.yaml)."
  - "0.1.0 — 2026-06-14 — first draft of the design and incremental plan."
  - "0.2.0 — 2026-06-14 — added YARA (file/memory matching) and Proofpoint, Microsoft 365/Entra adapters."
  - "0.3.0 — 2026-06-15 — Phase 0: analysis/ scaffolding created, common ECS schema defined (schema/common-schema.md), decisions made (ECS, Zircolite, Hayabusa, DuckDB); operational README with Phase 1 runbook. Code/tool execution not yet started."
  - "0.4.0 — 2026-06-16 — Phase 1 implemented: Hayabusa installed and wrapped (engine + adapter EVTX→ECS + ATT&CK report + CLI), validated on EVTX-ATTACK-SAMPLES (mimikatz → T1003.001). See README."
  - "0.5.0 — 2026-06-16 — PCAP/tshark adapter (network fields of the common schema): conversations/flows/DNS + ATT&CK signals as hypotheses (T1571/T1071.004/T1041), with test on deterministic synthetic pcap."
  - "0.6.0 — 2026-06-16 — §12: design of external threat intel enrichment (VirusTotal no-upload, Shodan), with egress gate as core constraint; Phase 6 added to the roadmap. Design only, no code."
  - "0.7.0 — 2026-06-21 — Phase 2 implemented: DuckDB in-memory store (analytics/store.py), long-tail recipes (process stacking, first-seen, rare parent-child, top talkers, rare DNS, non-standard ports, beaconing) and cross-source correlation (timeline, host overview, shared indicators), CLI engine/run_analytics, test on synthetic records."
  - "0.8.0 — 2026-06-21 — Local GUI (analysis/gui, FastAPI+HTML), baseline/diffing (analytics/baseline.py), case management (analytics/case.py + template §17), YARA adapter (adapters/yara_scan.py, optional dep) and threat intel enrichment (tools/enrichment, Shodan/VT egress-gated). All with tests (YARA skip if absent). Compliance mapping in tools/compliance (GDPR/NIS2/DORA)."
  - "0.9.0 — 2026-06-22 — Phase 3 SUSPENDED: the CrowdStrike export adapter has no input (no detection export available in any form); no speculative code built on unverifiable schema (§6/§7). CrowdStrike remains consulted in conversational mode (console/copied text, §8). To resume if/when an export or API credentials become available."
  - "0.10.0 — 2026-07-20 — principle 7 (CLI-first: tools are usable without AI, AI is the interpretation layer on top); §13 Swiss-army-knife roadmap: EvtxECmd spike succeeded on Mac via dotnet (full EVTX stream → fills the blind spot of the DuckDB engine on Hayabusa-only detections), adapter to build; future scopes (generic logs + host artifacts EZ tools) per-case; logon 4624/4625 view added to the engine (engine/run_logons)."
  - "0.11.0 — 2026-07-20 — correlation/GUI/report block IMPLEMENTED: SOT channel-aware EID map (adapters/windows_eventid, ~100 EID); EvtxECmd adapter (evtx_evtxecmd, source evtx_full, --evtx-full); generic log adapter (logfile: access/jsonl/regex/line) + engine/run_logs; extended correlation (temporal episodes + cross-family indicators + graph in correlate.py); visual HTML report (engine/report_html + run_report, SVG long-tail chart); multi-source GUI + EvtxECmd checkbox + downloadable report; extended schema (event.code/outcome, url/http, file.*). All CLI-first and tested."
  - "0.12.0 — 2026-07-20 — three new capabilities: (1) Zeek adapter (pcap_zeek.py, Zeek TSV parser → common schema, conn/dns/http/ssl/notice log, automatic integration in the PCAP pipeline alongside tshark, cross-source dedup), (2) SigmaHQ community rules (3142 rules in sigma/community/sigmahq, coverage IIS/DNS/DHCP/AD/Kerberos/cert services/Linux/macOS, + 2 custom rules in sigma/custom), (3) Decode engine (decode/: automatic recognition and decoding of Base64/hex/URL/ROT13/XOR/Base58/unicode escape, standalone CLI + tests)."
---

# EventHound — analysis and correlation engine

Design document. Defines a component for **analysis on real client data**, complementary to the RAG, supporting multiple products and workflow modes (EVTX event analysis, long tail analysis, cross-source correlation). Approach already decided: **wrap existing mature tools**, do not build a bespoke engine (no "homemade SIEM"). The tool is named **EventHound**.

## 1. Objective and boundaries

**Objective.** Given a set of heterogeneous security events (CrowdStrike export, Windows EVTX, Okta logs, ...), normalize them into a common schema, apply portable detection logic and local statistical analytics, and produce a report mapped to MITRE ATT&CK.

**Complementary to the RAG, not an alternative.**
- The **RAG** is the *knowledge* (what is a technique, how FQL works, what NIST says).
- The **engine** is the *analysis of real data* (this host, these events, this time window).
- Synergy: the engine **flags** (e.g. `T1003.001` on `HOST-01`), the RAG **explains and suggests next steps**.

**Out of scope.** It is not a real-time SIEM, does not do continuous streaming/alerting, does not replace the product console. Works in *batch/forensic* mode on provided datasets.

## 2. Principles

1. **Wrap, don't reinvent.** Detection on EVTX and rule format are solved problems: use Sigma + existing engines.
2. **Local and offline.** Everything runs on the machine; no data leaves. Consistent with confidentiality.
3. **Anonymized data.** Real events follow `method/anonymization.md`; output and datasets live in `data/` (private).
4. **Common schema.** Cross-tool correlation is only possible by reducing everything to uniform fields.
5. **Validation like in the RAG.** Just as `golden_queries` validate the index, a **detection test set** validates the engine's logic (dataset with known malicious events → the rule *must* fire).
6. **ATT&CK as lingua franca.** Every detection/analytic maps, where possible, to an ATT&CK tactic/technique.
7. **Usable without AI (CLI-first).** Like in PersonalFinance: the engine functions (EVTX import, log parsing, analytics, report, scoring) are **deterministic command-line tools**, executable and verifiable **without any model underneath**. AI is the *interpretation* layer above the output (explains, correlates, suggests next steps), not a dependency to run the tools. A tool that requires AI at every step becomes impractical: every new adapter/parser is born as a standalone CLI with its own test, and only *afterwards* leverages AI for interpretation.

## 3. Architecture

```
sources            adapter           common schema      store/compute      logic                 output
──────────         ─────────         ─────────────      ─────────────      ──────────────         ──────
EVTX            →  parser EVTX    →                  →                   →  Sigma (detection)   →  report
CrowdStrike     →  parser CS      →   ECS subset    →   DuckDB           →  long-tail analytics→  (Markdown,
Okta System Log →  parser Okta    →   ECS/OCSF       →   (embedded SQL)   →  correlation join   →   mapped
…                  …                  (norm. events)                         cross-source           to ATT&CK)
                                                                                                   →  validation
                                                                                                      (test set)
```

- **Per-source adapter**: convert native format into common schema records. Adding a product = adding an adapter.
- **Common schema**: subset of **ECS** (Elastic Common Schema) or **OCSF** — essential entities and event types (see §5).
- **Store/compute**: **DuckDB** embedded (local columnar SQL, reads parquet/csv/json, handles millions of rows). It is the core for long-tail and correlation.
- **Logic**: **Sigma** (vendor-agnostic rules) for detection on events/logs + **YARA** for **file/memory** matching (malware, binary artifacts) + library of parametric **analytics** in SQL (long-tail recipes). Sigma and YARA are complementary: the former looks at *what happened* (events), the latter at *what the artifact looks like* (content).
- **Report**: Markdown with timeline, (pseudonymized) entities, ATT&CK techniques, and references to the RAG for explanations.

## 4. Tools we wrap (decision: wrap)

| function | candidate tools | notes |
|----------|----------------|-------|
| Sigma detection on EVTX | **Zircolite** (Sigma→SQLite on EVTX, Python), **Hayabusa** (Rust, timeline), **Chainsaw** (Rust, WithSecure) | Zircolite is almost literally a "local Sigma correlation engine on EVTX"; Hayabusa excellent for fast timelines. **Decided in Phase 1 (2026-06-16): Hayabusa**, which covers detection and timeline in one binary. Zircolite is not used anywhere in this repository. |
| Rule format/conversion | **Sigma**, **pySigma** / **sigma-cli** | portable rules; conversion to backends (incl. SQL for DuckDB). |
| File/memory matching | **YARA** (`yara-python` / **YARA-X**) | rules for malware and binary artifacts; complementary to Sigma. Scan files collected via RTR, memory dumps, suspicious samples; map to families/IoCs. |
| Raw EVTX parsing (full stream) | **EvtxECmd** (Eric Zimmerman, .NET; field normalization *maps*), **evtx** (omerbenamram, Rust+Python), **python-evtx** | when raw data beyond detections is needed. EvtxECmd verified cross-platform on this Mac via `dotnet` (spike 2026-07-20, see §13). |
| Store/analytics | **DuckDB** | long-tail, stacking, cross-source join. |
| Test dataset | **EVTX-ATTACK-SAMPLES**, **OTRF Security-Datasets/Mordor** | known malicious events, mapped to ATT&CK, for validation. |

## 5. Common schema (draft)

Minimal subset, in ECS style, sufficient to correlate host/user/process/time:

- **common**: `@timestamp`, `event.source` (evtx|crowdstrike|okta), `event.action`, `event.category`, `attack.technique`.
- **host**: `host.name` (pseudonym), `host.os`.
- **user**: `user.name` (pseudonym), `user.domain`.
- **process**: `process.name`, `process.command_line`, `process.pid`, `process.parent.name`.
- **network**: `source.ip`, `destination.ip`, `destination.port`, `dns.question.name`.
- **auth**: `event.outcome` (success|failure), `logon.type`.

> ECS vs OCSF choice to be confirmed in Phase 0. ECS is leaner and more widely adopted in tooling; OCSF is newer and more standardized cross-vendor.

## 6. Supported workflow modes

- **EVTX event analysis** → EVTX adapter + Sigma (via Zircolite/Hayabusa) → events of interest (logon 4624/4625, process create 4688, PowerShell 4104, service install 7045, ...) with ATT&CK mapping.
- **Long tail analysis** → normalization in DuckDB + SQL recipes: frequency stacking / least-frequent-occurrence (rare processes, anomalous parent-child, "first access of USER to HOST", rare domains/URLs, rare UAs).
- **Cross-source correlation** → join in DuckDB on `host.name` / `user.name` / time window across different sources (e.g. CrowdStrike detection + EVTX on the same host).
- **File/artifact analysis (YARA)** → scanning files, executables or memory dumps with YARA rules to identify known malware and artifacts; output with family/IoC and, where possible, ATT&CK mapping.

## 7. Proposed structure of `analysis/`

```
analysis/
├── DESIGN.md            # this document
├── adapters/            # per-source parsers -> common schema (evtx, crowdstrike, okta, …)
├── schema/              # common schema definition (ECS/OCSF subset)
├── sigma/               # Sigma rules used/curated (vendored or submodule)
├── yara_rules/          # YARA rules for file/memory (malware, artifacts)
├── analytics/           # long-tail recipes and correlation in SQL/DuckDB
├── engine/              # orchestration: wraps tools, normalizes, executes logic
├── tests/              # detection test set (known datasets -> expected outcomes) [private if containing real EVTX]
└── reports/ -> data/    # reports on real cases (private, anonymized)
```

Privacy/git model: code/rules/analytics **versionable**; real datasets, client EVTX and reports **private** (`data/`, gitignored). Public test datasets (EVTX-ATTACK-SAMPLES) can be versioned or referenced.

## 8. Validation (common thread with the RAG)

Mirror of `golden_queries`: a **detection test set** (for Sigma and YARA rules). For each known dataset (e.g. an EVTX from EVTX-ATTACK-SAMPLES containing Kerberoasting, or a sample a YARA rule must recognize), the expected outcome is declared (must fire `T1558.003` / the expected YARA rule). A runner executes the engine and verifies PASS/FAIL. Same idea: automatically validate the *soundness of the logic* when adding or modifying a rule/analytic.

## 9. Incremental implementation plan

Vertical and narrow, to deliver value early and avoid building the "universal engine" on the first attempt.

> Status (2026-06-16): **Phase 1 implemented**. **ECS** schema, toolchain installed
> (**Hayabusa** + EVTX-ATTACK-SAMPLES dataset + Zircolite clone, in `.tools/`),
> EVTX→ATT&CK slice working and validated (mimikatz sample → `T1003.001`). See
> `README.md` for usage and components. Next: Phase 2 (DuckDB + long-tail). Note:
> for Phase 1, **Hayabusa** was chosen as the first detection engine (it comes with
> Sigma rules with ATT&CK mapping, the most direct path to the report); **Zircolite**
> remains cloned for alternative/complementary Sigma detection in a later phase.

- **Phase 0 — Foundations.** Choose ECS vs OCSF and Zircolite vs Hayabusa; define the minimal common schema; set up `analysis/` scaffolding; obtain 1 anonymized EVTX example + 1 known test dataset.
- **Phase 1 — EVTX → detection slice.** Wrap the chosen Sigma tool on an EVTX, produce a report mapped to ATT&CK. *(This is the first concrete increment.)*
- **Phase 2 — DuckDB + long-tail layer.** *(implemented, 2026-06-21)* DuckDB in-memory store on common schema records (`analytics/store.py`); long-tail recipes (process stacking, first-seen host/user, rare parent-child, top talkers, rare/long DNS, non-standard ports, beaconing) and cross-source correlation (timeline, host overview, shared indicators). CLI `engine/run_analytics`, test on synthetic records.
- **Phase 3 — Second adapter + correlation.** *(DONE, 2026-07-24)* CrowdStrike export adapter (`adapters/crowdstrike.py`: detection clipboard `Key: Value` blocks + LogScale/CQL JSON incl. the `@rawstring` wrapper) and cross-source EVTX↔CrowdStrike join on host/user/time, which follows from the common schema (`analytics/correlate.py`). It was unblocked by a different input than the one it waited on: no detection *export* exists, but a detection copied out of the console is a real, checkable sample, so nothing was written on guessed field names (§6/§7). **Note (2026-08-27):** the *documentation* half of the CrowdStrike strand is gone — `cs_falcon_docs` and `docs/crowdstrike/` were removed and product questions moved to a separate project on the official MCP server. The adapter is unaffected.
- **Phase 4 — Automatic validation.** Detection test set + runner; integration into the workflow (like the RAG auto-eval): modify a rule → test runs.
- **Phase 5 — More products / YARA.** Okta System Log, **Microsoft 365 / Entra** (Entra sign-in logs, Defender for Office 365 / Exchange Online Protection), **Proofpoint** (email security / antispam), Microsoft Defender/Sentinel, etc., reusing schema and pattern *(product adapters: to do)*. **YARA implemented** (2026-06-21): `adapters/yara_scan.py` (file→common schema, technique from meta), optional dep `--extra yara`, test rule in `analysis/yara_rules/`.
- **Phase 6 — External threat intel enrichment.** *(implemented, 2026-06-21)* `tools/enrichment/`: Shodan InternetDB (keyless) + VirusTotal (keyed, lookup only). Egress gate (§12) and blocking of non-public indicators (RFC1918 IPs, internal domains) even with egress active. MCP tools `shodan_lookup`/`vt_lookup`.
- **Cross-cutting (2026-06-21).** Local GUI (`analysis/gui`), **baseline/diffing** (`analytics/baseline.py`: new vs known-good), **case management** (`analytics/case.py` + template §17), **compliance mapping** (`tools/compliance`: incident→GDPR/NIS2/DORA obligations).

## 10. Open decisions / risks

- **ECS vs OCSF**; **Zircolite vs Hayabusa vs Chainsaw** (likely combination: timeline + detection).
- **Licensing/usage** of test datasets and tools (verify before vendoring).
- **Performance** on large EVTX (DuckDB handles it; EVTX parsing is the bottleneck → prefer Rust parsers).
- **Event anonymization**: real EVTX contain hostnames/users/IPs → must be pseudonymized before shareable reports; evaluate post-parsing anonymization in the common schema.
- **Boundary with the console**: the engine complements, does not replace, native product analysis.

## 11. References

Sigma, pySigma; Zircolite, Hayabusa, Chainsaw; DuckDB; ECS (Elastic), OCSF; EVTX-ATTACK-SAMPLES, OTRF Security-Datasets; MITRE ATT&CK; **VirusTotal API v3**, **Shodan API / InternetDB** (for §12). To be validated and linked in `method/fonti/riferimenti.md`.

## 12. External threat intelligence enrichment (VirusTotal / Shodan) — IMPLEMENTED

Third layer, complementary to the existing two: the engine **flags** an indicator in an
ECS record (a hash, a `destination.ip`, a `dns.question.name`), enrichment **annotates** it
with reputation and exposure, the RAG **explains** the technique. It does not replace
either; it adds external context to the indicator.

### What is integrated
- **VirusTotal — no upload.** Read-only *indicator lookup*: `GET /files/{sha256|md5}`,
  `/domains/{domain}`, `/ip_addresses/{ip}`, `/urls/{id}` (API v3). No `POST /files`:
  no sample submission, no file exposure to third parties. Useful output:
  `last_analysis_stats` (malicious/suspicious), reputation, categories.
- **Shodan.** `GET /shodan/host/{ip}` (ports, services, banners, CVEs) for **public IPs**;
  to start without keys, **InternetDB** (`internetdb.shodan.io/{ip}`, keyless): ports,
  CPEs, known CVEs, hostnames. On internal IPs Shodan has nothing.

### Core constraint — egress gate
Enrichment **breaks** the "local and offline, no data leaves" principle (§2): every
query sends an indicator to a third party. Therefore it is subject to strict rules, analogous to
§15 of `method/conventions.md` (*outward* action, opt-in, to be confirmed):

- **Only non-client, public indicators may leave**: malware hashes, public malicious IPs/domains,
  CVEs — the same that §9 says *not* to pseudonymize.
- **Never client identifiers**: internal IPs/hosts, usernames, corporate domains, tenant
  IDs. Sending them to VT/Shodan is a leak to third parties.
- An **indicator classifier** decides "sendable vs client-sensitive"; when in doubt,
  **do not send**. Consistent with `method/anonymization.md`.

### Proposed form (when implemented)
- Module `tools/enrichment/` **downstream** of normalization (not an adapter: adapters
  bring data *in*, enrichment *annotates* already normalized records).
- Extends records with a dedicated namespace (e.g. `threatintel.vt.*`, `threatintel.shodan.*`)
  without touching the common subset (§5, extensibility rule).
- **API keys** in a gitignored file (`data/config.json`, overridable with `EVENTHOUND_CONFIG`), never committed. Shared by the GUI and the CLI through `tools/eventhound_config.py`, which masks them on read.
- **Local cache** of responses (in `data/`, private): respects rate limits (VT free
  ~4 req/min, 500/day; Shodan by credit; InternetDB free) and avoids re-querying.
- **Dedup + batch** of indicators before calls.
- Activation via `--enrich` flag on `run_evtx`/`run_pcap`, **off by default**; the report
  annotates reputation/exposure next to the indicator, with source and lookup date.

### Recommended starting point
First establish the rule for *what can leave* (above), then a **keyless** pass with
Shodan InternetDB + the skeleton of the indicator classifier and cache; VirusTotal
is wired in when an API key becomes available. Endpoint/rate-limit details to be confirmed
against official VT/Shodan docs before finalizing (§6).

## 13. Roadmap — standalone parsers and multi-artifact adapters (the "Swiss Army Knife")

The vision: a multi-purpose tool for heterogeneous cyber scenarios. The way to get there **without sprawl** is the backbone that already exists — **one common schema + one DuckDB engine, fed by a growing set of adapters** — and principle 7 (§2): every adapter is a **deterministic standalone CLI**, usable without AI. EVTX (Hayabusa) and PCAP (tshark) are the first spokes; others are added **one case at a time**, when actually needed (§7 minimum complexity, §18 product addition).

### 13.1 Next concrete spoke: EvtxECmd adapter (full EVTX stream)

**Why, not redundant with Hayabusa.** Hayabusa provides **detections** (Sigma rules + ATT&CK); EvtxECmd provides **all normalized events**, not just those flagged. Today the DuckDB engine only sees Hayabusa detections: the long-tail recipes (`rare_processes`, `first_seen`, `beaconing`) are **blind** to everything Sigma did not flag. Feeding EvtxECmd's full stream into the **same common schema** closes this gap — it is the engine's missing half.

**Spike outcome (2026-07-20).** EvtxECmd (.NET 9) **runs on this Mac** via `dotnet` with `DOTNET_ROLL_FORWARD=Major` (runtime .NET 10 present). On the sample `LM_WMI_4624_4688_TargetHost.evtx` it parsed **all 8 records** (6× 4624 + 2× 4688 — including the 4688 that a `logon-summary` would not show) in 0.36 s. Output is JSON *line-delimited* with **BOM** (read as `utf-8-sig`), per-record stable schema: `TimeCreated`, `EventId`, `Channel`, `Computer`, `Provider`, **`MapDescription`** (human-readable label from *maps*, e.g. "Successful logon"), `UserName`, `RemoteHost`, **`PayloadData1..6`** (the *maps* extract key fields, e.g. `Target: EXAMPLE\WIN...`, `LogonType 3`, `LogonId`), plus raw `Payload`. Binary in `analysis/.tools/evtxecmd/` (gitignored, like Hayabusa).

**DONE (2026-07-20, v0.11.0).** `engine/evtxecmd_runner.run_evtxecmd` (dotnet + roll-forward) → `adapters/evtx_evtxecmd.py` (JSON EvtxECmd → schema, parsing canonical EventData `Payload` + fallback `PayloadData*`, EID map from SOT `windows_eventid`), source `evtx_full`, flag `--evtx-full` on `run_analytics`/`run_report` and checkbox in the GUI. Long-tail recipes see the full stream. Test `tests/test_evtxecmd.py` (synthetic + LM_WMI, auto-skip without dotnet). Alongside these were implemented: the **generic log adapter** (`adapters/logfile.py` + `engine/run_logs.py`), **extended correlation** (temporal episodes, cross-family indicators, graph — `analytics/correlate.py`), **visual HTML report** (`engine/report_html.py` + `engine/run_report.py`) and **GUI wiring** (multi-source + downloadable report).

### 13.2 Sigma community rules and decode engine

**Sigma community rules.** Hayabusa has ~5000 Sigma rules but they are skewed toward process_creation (1402) and registry (233). SigmaHQ rules in `sigma/community/sigmahq/` (3142 rules) fill gaps in: IIS web server, DNS server, DHCP, AD replication/Kerberos, certificate services, print spooler, file share access, Linux/macOS. Used with Hayabusa: `hayabusa csv-timeline -r sigma/community/sigmahq/rules -r sigma/custom/detection`. Project custom rules live in `sigma/custom/` (versioned).

**Decode engine.** Automatic recognition and decoding of common encodings in security logs: Base64, hex, URL encoding, ROT13/ROT-N, XOR single-byte, Base58, Unicode escape. Standalone CLI (`engine.run_decode`), applicable to command lines, DNS queries, HTTP bodies, log messages. No external dependencies (only stdlib Python). The decoder is defensive: it never executes the decoded content, only displays it.

### 13.3 Zeek integration (network analysis alongside tshark)

**Why Zeek alongside tshark.** tshark extracts raw fields from packets (IP, ports, protocol); Zeek provides structured application-layer visibility: HTTP (URL, user-agent, status code), SSL/TLS (SNI, JA3, certificates), DNS (responses, rcode), file transfer, notice/alert. Together they cover both the packet layer and the application layer.

**Implementation.** `adapters/pcap_zeek.py` runs `zeek -r <pcap> -C` in a temporary directory, parses TSV logs (conn.log, dns.log, http.log, ssl.log, notice.log) and maps fields into the common schema. The PCAP pipeline (`engine/run_pcap.py` + `analytics/runner.py`) automatically runs both adapters (tshark + Zeek) and merges results per connection. Zeek error is non-fatal (silent skip if not installed).

**Zeek log → common schema field mapping:**
- conn.log → source.ip, destination.ip, destination.port, network.transport, event.outcome
- dns.log → dns.question.name, dns.response_code
- http.log → url.domain, url.path, http.request.method, http.response.status_code
- ssl.log → tls.server_name, tls.ja3, tls.version
- notice.log → rule.description, rule.name

### 13.4 Registry and MFT spokes (RECmd, MFTECmd) — implemented (2026-07-21)

The first two host-artifact spokes beyond EVTX/PCAP, both Eric Zimmerman .NET tools wrapped like EvtxECmd (`dotnet` + `DOTNET_ROLL_FORWARD=Major`):

- **Registry.** Two paths into the common schema: native `.reg` export parsing (`adapters/registry_regfile.py`, no external tool needed, wired into the main analytics pipeline and GUI multi-source upload) and live-hive parsing via **RECmd** (`engine/recmd_runner.py` + `adapters/registry_recmd.py`), which dumps all keys/values and filters Auto-Start Extension Points (ASEP) for persistence detection. RECmd has a dedicated `POST /api/registry` GUI endpoint (separate from `/api/analyze`, since ASEP scanning is a distinct workflow from long-tail/correlation).
- **MFT.** `engine/mftcmd_runner.py` + `adapters/mft_mftecmd.py` wrap **MFTECmd** ($MFT parser: file name/path, MACB timestamps from `$STANDARD_INFORMATION`/`$FILE_NAME` attributes, size, allocation, deleted flag) into the common schema. Adapter and runner are implemented and validated (`tests/test_mft.py`, synthetic + end-to-end auto-skip without `dotnet`/dll); the GUI health-checks `mftecmd` availability but does not yet expose a dedicated analysis endpoint — that wiring is the next step when a case needs it.

### 13.5 Future spokes (per-case, not now)

The rest of the DFIR ecosystem — always as CLI adapters toward the same schema, added when a real case requires them:

- **Host artifacts (Eric Zimmerman tools, all CLI .NET)**: Prefetch (**PECmd**), LNK/JumpList (**LECmd/JLECmd**), **Amcache**, SRUM (**SrumECmd**). Each one an adapter toward the schema; they expand the engine further into full host forensics (Registry and MFT are now implemented, §13.4).

### 13.6 Hayabusa toolbox — triage/hunting beyond Sigma detections

Hayabusa's other subcommands, beyond `json-timeline` (Sigma detection), give frequency/orientation and keyword/regex hunting: `engine/hayabusa_runner.py` exposes them via `run_command()` — `eid-metrics`, `log-metrics`, `computer-metrics` (which EIDs/channels/hosts, how often), `search` (keyword/regex over the full stream), `pivot-keywords-list` (IOC extraction to pivot on), `extract-base64`. Plus `run_logon_summary` (the dedicated 4624/4625 view, §9 Phase 2). In the GUI these are six `POST /api/hayabusa/*` endpoints living inside the EVTX view (single shared upload), not a separate view. `docs/analysis/threat-hunting-evtx.md` is the reading key: per-EID hunting patterns (credential access, lateral movement, persistence, ...) mapped to the toolbox command that surfaces them, cross-checked against the SOT EID map (`adapters/windows_eventid.py`) and Microsoft auditing references.

### 13.7 Containerized packaging

The whole stack runs via `docker compose` from the repo root (`docker-compose.yml`): `qdrant` (vector DB, reuses `rag/qdrant_storage`), `rag-api` (retrieval as an HTTP service, warm models, `rag/rag-api.Dockerfile`), and `eventhound` (`analysis/eventhound.Dockerfile`, `python:3.12-slim-trixie`) — a single self-contained image with **every** tool baked in (tshark, Zeek, Hayabusa, `dotnet` runtime + EvtxECmd/RECmd/MFTECmd) so runners discover binaries via `PATH`/`.tools/` with no code change. The GUI reaches the RAG via `RAG_API_URL=http://rag-api:8600` (the HTTP path already in `app.py`, §gui). Bound to `127.0.0.1:8700` only (client identifiers, §9/§10), never exposed on the network. Code is baked into the image (no volume mount): rebuild to pick up changes.

### 13.8 Out of scope (for now)

- **Timeline Explorer** — it is a Windows GUI for grouped timeline; that role is already covered by the local GUI + DuckDB.

## 14. Local conversational AI engine (on-box LLM) — REMOVED 2026-09-01 (superseded by `analysis/analysis_mcp_server.py`)

The **third surface** of the product (§1 of the root README: 100% GUI, 100% CLI, **100% local AI**). Today the interpretation layer is *external* (Claude Code / Mistral Vibe, i.e. development tools, §1). This section designs the engine that ships *inside* EventHound and runs **on-box** — so an analyst with no external AI still gets reasoning, assessment and remediation paths over their own analysis, fully offline.

**Built on 2026-07-21** in the five tested steps §14.8 lays out: the AI layer (ReAct loop over Ollama native tool-calling, context packer, anonymization gate, tools), the `run_ai` CLI, and the three GUI endpoints. The section is kept as written because it is the design the implementation followed and the reasoning is still the reasoning; where it says what *will* be done, read what *was*.

### 14.1 What it is — and is not

- **It is** an *interpretation layer* over data the deterministic engine has **already produced**: a long-tail table, a correlation graph, a Hayabusa search result, a detection, a report. It reasons over that context, grounds the reasoning in the RAG, and proposes **insight / assessment / mitigation / next analysis steps**.
- **It is not** a new analysis engine. It does not parse EVTX, does not compute scores by hand (§6 — it *calls* the scoring oracle), does not run detections itself. The sensors + DuckDB + Sigma remain the source of the facts; the LLM explains and connects them.
- **It proposes, never executes** (§12): any command/query it suggests (RTR, PowerShell, FQL, a follow-up `run_*`) is surfaced as text for the analyst to run in their authorized environment — the engine has no tool that acts on real systems.

### 14.2 Architecture — CLI-first, GUI as a thin layer

Consistent with principle 7 ([[cli-first-usabile-senza-ia]]) and the enrichment/RAG precedent: a module callable headless, with the two GUI surfaces a thin shell over it.

```
analysis/ai/
  engine.py        # the orchestration loop (prompt → tool calls → grounded answer)
  ollama_client.py # thin wrapper over the Ollama HTTP API (OLLAMA_URL, default 127.0.0.1:11434)
  tools.py         # the read-only tool registry exposed to the model (§14.3)
  context.py       # context packer: analysis output → bounded digest (§14.4)
  redact.py        # anonymization gate before any prompt (§14.5), reuses method/anonymization
run_ai.py          # CLI: `run_ai --context <analysis.json> --ask "..."` and interactive REPL
```

- **CLI** (the real engine): `run_ai` takes a context source (a saved analysis/report JSON, or `--rag-only` for a pure knowledge question) and a question, returns a grounded answer with citations. Usable in a shell, no GUI, no external AI.
- **GUI chat**: a dedicated view = a thin `POST /api/ai/chat` (SSE stream, like `/api/analyze`) over `engine.py`.
- **GUI "AI button"**: each function's output panel gets a button that POSTs *that function's already-computed output* as the context to `/api/ai/explain`. No re-computation — it hands the existing result to the engine as context (this is why the packer §14.4 works on structured output, not raw artifacts).

### 14.3 Model choice and tool-calling protocol (decision)

**Model — decision: Ollama, model configurable, never hardcoded; default `qwen2.5:7b-instruct` (2026-08-29).** Requirement is **reliable native tool-calling**: grounding and scoring have to actually run (§6). From 2026-07-21 the default was `qwen2.5:14b`, on two real multi-tool queries in which it emitted tool calls where the 7B narrated them in prose. That was two data points, and `run_toolbench` was built to replace them with a rate; when it did, the rate went the other way — 7b-instruct 54/60 against 44/60, zero non-Latin-script answers against 7 of 60, 2.2x faster, with the 14b ahead only on narration by one event in 42 (`docs/analysis/performance.md`). The default now follows the corpus, on every host: `default_model()` no longer branches on RAM, because that branch answered the *hardware* question while leaving the *quality* one open, and a large host deserves the better model too.

**Precedence**, resolved once in `ollama_client.configured_model()`: `EVENTHOUND_LLM_MODEL` → the stored `llm_model` setting → `default_model()`. The middle step is the **Assistant's model picker**, which writes the choice to the shared store (`tools/eventhound_config.py`), so the 14b — or any pulled model — is selected in the GUI and used by the CLI, the MCP tools and the next session as well. Selecting a model this host cannot hold is allowed and answered honestly at the moment of choice (`memory_status`): the guard belongs on the load, not on the setting. The shell asks the same seam (`setup.sh model`, `uninstall.sh --purge-models`) so what gets pulled is what gets used. The engine stays **model-agnostic** — no prompt tuned to one model's quirks; a startup check confirms the model is pulled.

**Tool-calling — decision: use Ollama's native tool-calling (`/api/chat` `tools` field), wrapped in a *small* orchestration loop we own** (`method/minimal-code.md` rung 4-7: the platform feature does the calling; we own only the thin loop + registry). The loop is the classic bounded ReAct: send question + tool schemas → model emits a tool call → we execute the (read-only) tool → feed the result back → repeat until the model answers or a **hard call cap** (e.g. 6) is hit. No agent framework, no LangChain (rung 5: no dependency for what a `while` loop does).

**The tools exposed are read-only and already exist** — the registry is a thin adapter over current capabilities, not new engines:
- `rag_search(query, collection)` — the grounding tool (mandatory before any technical claim, §6). Reuses the `rag-api` HTTP service.
- `cvss/risk/epss` — the scoring oracle (`tools/scoring/`), so numbers are never invented (§6).
- `enrichment_lookup(indicator)` — Shodan/VT/ThreatFox, **egress-gated and public-indicators-only** (§14.5, same gate as `tools/enrichment/`).
- `query_analysis(...)` — read-only DuckDB queries over the *current* analysis store (top long-tail rows, an episode, events for a host) so the model can pull detail on demand instead of holding it all in context.

No tool in the registry mutates state or touches a real system (§12). Enrichment is the only one that egresses, and only through the existing gate.

### 14.4 Context budget — the packer

Local models have a small window (8k–32k). Analysis outputs (long-tail tables, correlation graphs, full timelines) do **not** fit raw. `context.py` turns each function's output into a **bounded structured digest** before it enters the prompt: top-N rows by signal, counts/aggregates instead of full tables, the key indicators (IP/user/host/hash) rather than every event, ATT&CK IDs already assigned. Detail is fetched *on demand* via the `query_analysis` tool (§14.3) rather than pre-loaded — the model asks for the rows it needs. This keeps the resident context small and pushes bulk to tool calls, which is also what makes the small-window model viable.

Two things go in as **ordered sequences** rather than as top-N row lists, because their value to the model is the order and a frequency table destroys it: the unified timeline, and the **kill chain** — phases in order with their tactics, techniques and hosts, the per-host triage depth, the technique catalogue, and each phase's corroboration carried through with the label it is given everywhere else (co-occurrence in time, never a technique). The kill chain was *absent* from the digest until 2026-08-29: it was named in the packer's "not a recipe" set, which removes a key from the generic block and does nothing else, so the assistant — one of the three declared interfaces (§1) — was the only surface that could not see how far the activity reached, against four renderers that could.

### 14.5 Anonymization gate — before any prompt (non-negotiable, §9)

Even though the model is **local**, client data is pseudonymized **before** it enters a prompt — same discipline as a shared report (§9, [[sma-log-format-e-adapter]]). `redact.py` applies the pseudonym map (`data/pseudonym-map.md`) to the analysis **before** it is packed, so the LLM reasons over `HOST-01`/`USER-03`, never real identifiers. The order matters and this section used to describe the wrong one: `Redactor` substitutes exact substrings, so redacting the *packed* text leaves any identifier the packer's own truncation split unmatched — `SRV-FINANCE-PROD-01` cut mid-name reaches the model intact. All three callers redact first; `run_toolbench` was the last one doing it the other way round (fixed 2026-08-29). Two reasons it still matters on-box: (1) the analyst may copy the AI's answer into a shared deliverable; (2) defense in depth — the model must never *learn* to echo real identifiers. The enrichment tool keeps its own **egress gate**: only public, non-client indicators may leave the machine; the indicator classifier (§12) decides sendable-vs-sensitive, and when in doubt does **not** send. The LLM cannot bypass this — it calls the same gated enrichment function, not raw HTTP.

### 14.6 Grounding, citation, and honesty (system prompt contract)

The system prompt hard-codes the method (§6): ground every technical claim in a `rag_search` result and **cite the source**; get security numbers from the scoring tool, never compute them; mark anything not backed by a source as *hypothesis / to be verified*; **propose** commands, state their impact, never present them as executed (§12); prefer non-invasive triage steps first. A claim the RAG cannot support is flagged as a coverage gap, not fabricated. This is the same contract these governance files impose on the external assistants — now enforced in-product.

### 14.7 Service pattern (decision)

**Decision: Ollama is its own compose service, consistent with the rest of the stack.** The project is fully containerized (`docker-compose.yml`: `qdrant` + `rag-api` + `eventhound`), so the LLM follows the same model — a dedicated **`ollama` service** (image `ollama/ollama`, loopback `127.0.0.1:11434`, model store persisted in a gitignored `./ollama_storage` volume), added alongside the others (implemented in `docker-compose.yml`, 2026-07-21). We do **not** build our own `llm-api` wrapper service: unlike the RAG (which had no standalone server, hence `rag-api`), Ollama already *is* an HTTP server — wrapping it would be redundant (`method/minimal-code.md`). So `ollama_client.py` is a thin HTTP wrapper inside the `eventhound` image; `engine.py` orchestrates. The `eventhound` service reaches it via `OLLAMA_URL=http://ollama:11434` (compose network); for host dev without Docker it defaults to `http://127.0.0.1:11434`.

**Notes.** The model is **not** baked into the image (too heavy) — it is pulled once into the volume (`docker exec eventhound-ollama ollama pull $EVENTHOUND_LLM_MODEL`) and persists. **Hardware:** on Docker Desktop for macOS containers have **no GPU/Metal access** → the containerized Ollama runs CPU-only (slower than host Ollama, which uses Metal); on a Linux host with NVIDIA the commented `deploy` GPU block in the service enables acceleration. The engine stays optional (§14.9): the other services run fine whether or not a model is pulled.

### 14.8 Incremental plan (when built)

1. **Skeleton, RAG-only, CLI — DONE (2026-07-21).** The package (ollama_client.py thin HTTP client over `OLLAMA_URL`; `tools.py` read-only registry with `rag_search` reusing the GUI's rag-api-HTTP→RAG-venv-subprocess fallback; `engine.py` the owned ReAct loop + the §14.6 system-prompt contract) + the `run_ai` CLI (`run_ai "question"`, `--model/--url/--show-calls/--json`, degrades with guidance + exit 3 when Ollama/model absent). A test pinned the loop offline with a scripted fake client (dispatch, result feedback, JSON-string args, unknown-tool recovery, call-cap→forced answer, RAG compaction) + an e2e that auto-skips without a pulled model; wired into `tools/check.sh` (green). No analysis context, scoring/enrichment tools or redaction gate yet — those are steps 2-3.
2. **Context packer + `query_analysis` — DONE (2026-07-21).** `ai/context.py` (`load_analysis` + `pack_analysis`: summary + top-N rows per non-empty recipe + correlation highlights, bounded by a char budget with an explicit truncation note, raw `records` never dumped). Two read-only tools added to the registry: `query_analysis` (parameterized ops `events_for_host`/`events_for_user`/`by_event_code`/`search` over a DuckDB store rebuilt from the saved `records` — no free-form SQL, injection-safe by construction; gated on a loaded context) and `eid_lookup` (Windows Event ID → the channel-aware SOT `windows_eventid`, unmapped→"verify", so the model stops confabulating EID semantics — the concrete §14.9 fix). `run_ai --context <analysis.json>` loads+packs+wires it; `engine.run(context=…)` injects the digest as data-to-interpret (prompt-injection note in-line). Tests extended (packer budget, all query ops, eid mapped/unmapped, registry gating). Validated e2e on a synthetic analysis: the model called eid_lookup and reasoned over the context. NB (§14.9): the 7B model sometimes *describes* a tool call in prose instead of emitting it — a model-quality limit, not a loop bug (the loop/tools are test-pinned); mitigated by a stronger model or step-3 tightening.
3. **Scoring + enrichment tools + redaction gate — DONE (2026-07-21).** `ai/redact.py`: map-based anonymization gate reading `data/pseudonym-map.md` (the same SOT as check-leaks, identical column/placeholder filtering) — case-insensitive, longest-first substitution, recursive over the loaded analysis (context digest AND the records backing query_analysis, so the whole downstream stays in pseudonym space). No heuristic guessing (a heuristic can't tell a client IP from a public malicious one, §9). Empty/absent map → inactive no-op with an explicit CLI warning (§9 honesty — an empty map is not a safe map). Wired into `engine.run(redactor=)` (redacts question + context + every tool result before the model) and `run_ai` (redacts the analysis at load; `--enrich` opt-in). Registry gained `cvss_score`/`risk_score` (the deterministic oracle, numbers never by hand §6) and `enrich_indicator` (egress-gated, public-indicators-only — the oracle's own guard refuses private/client indicators even here, §9). Tests extended incl. the §14.5 property: **a real identifier never reaches the model in plaintext** (question + context + tool output all pseudonymized). e2e-validated. Steps 4-5 (GUI chat + AI button) remain.
4. **GUI chat — DONE (2026-07-21).** `POST /api/ai/chat` (SSE) — a thin `_ai_stream` over `ai.engine.run` mirroring `_analyze_stream` (queue + executor so the blocking loop doesn't stall the event loop); emits a `start` event (model + redaction status), live `tool` events, then `complete`. Anonymization gate applied server-side (§9); an optional `context` (a full analysis result) is packed + records wired for query_analysis. Frontend: an **Assistant** view (sidebar + chat UI, `#ai-messages`/`#ai-input`, "use current analysis" checkbox gated on `lastResult`), a reusable POST-SSE consumer (`aiStream`), a `redaction_active` notice, and SAFE rendering of untrusted model output (escape + fenced-code only). Existing `test_gui` contract preserved; new AI test passes both with a pulled model and as a clean skip.
5. **Per-function AI button — DONE (2026-07-21).** `POST /api/ai/explain` (SSE) hands one function's already-computed output to the engine as context. Reusable `aiExplain(fn, output)` / `aiButton(fn, getOutput)` helpers; wired on the dashboard ("AI · explain this analysis" → the whole result) and the Correlation Highlights panel ("AI" → episodes/shared-indicators/host-overview). Same anonymization gate and SSE path as chat; extend to more panels (Hayabusa search, a detection) by dropping in `aiButton` — the pattern is in place.

Each step is CLI-first and tested before the GUI layer; no step ships without the anonymization gate once real context is involved.

**Refinements — DONE (2026-07-21).** (a) **Token streaming**: `ollama_client.chat_stream` (NDJSON) + `engine.run(stream=True)` emit `token` events; the loop is unchanged (a `_turn` helper streams or blocks). GUI forwards `token` SSE (live plain-text in the bubble, replaced by the rendered final on `complete`); CLI `run_ai --stream`. Tool-call turns carry no tokens; only the answer streams. (b) **Model picker**: `GET /api/ai/models` (Ollama `/api/tags` + default) feeds a GUI dropdown; the selected model rides the `model` field the `/api/ai/*` endpoints already accept (also how the tests smoke on a fast model without changing the 14b default). Both tested (`test_ai` streaming path with a fake streaming client; `test_gui` models endpoint).

### 14.9 Open decisions / risks

- **Small-model reliability — the mitigation was the wrong model, and measuring said so.** 7–8B tool-calling proved shaky in two ad-hoc queries (the 7B narrated tool calls in prose instead of emitting them, and confabulated EID 4656), and the mitigation applied was to default to the 14b. The corpus then measured both and reversed it: the default is **`qwen2.5:7b-instruct`** since 2026-08-29 (§14.3), with the 14b selectable. The loop is model-agnostic so the model is a swappable lever, and the hard call cap + the read-only registry bound the blast radius. A wrong security claim has real cost (§6) — the engine must **degrade to "insufficient grounding"** rather than confabulate; `eid_lookup`/`rag_search` returning "unmapped/no support" is the honest path. **That decision rested on two ad-hoc queries until 2026-07-24**, when `eval/toolcall_prompts.json` + `engine/run_toolbench.py` turned it into a rate: 20 labelled prompts, 14 requiring a named tool, replayed N times per model, separating *did not call* from *narrated a call it never made*. Figures and the residual failure modes are in [`docs/analysis/performance.md`](../docs/analysis/performance.md); the harness needs Ollama, so its test skips honestly while the scoring logic is pinned offline against a scripted client (`tests/test_toolbench.py`).
- **Latency.** On CPU, a 7–8B model + several tool round-trips is seconds-to-tens-of-seconds (cf. the ~8s RAG cost, [[eventhound-rebrand]]). Acceptable for forensic/batch use (§1, not real-time); stream tokens in the GUI so it feels responsive.
- **Hardware floor — was documentation, is now enforced (2026-08-28).** This bullet used to read "document
  a minimum". It was never documented and never checked, and on 2026-08-28 the gap was collected: Ollama
  loaded a 9.1 GiB model onto a 16 GB Mac with 2.3 GiB free and swap at 92%, and the graphical session died.
  Ollama had logged `system_limited=true` and loaded regardless — the failure was ours for not looking, not
  its for telling us. the memory guard now measured RAM, available memory and swap pressure;
  `default_model()` answers with the model the corpus supports (§14.3, one answer on every host); `memory_preflight()` refuses a load that would not
  fit, inside `chat`/`chat_stream` so no caller can forget it. The RAG side follows the same rule and
  learned it the hard way: its guard sat in `retrieve`'s three lazy getters, which covered queries and
  left `evaluate` and `ingest` — the heaviest load of the three — constructing embedders straight past
  it, and the `rag-api` image did not contain `tools/` at all, so there the guard was permanently
  inert. Since 2026-08-29 it lives in `Embedder`'s constructor, so the dense model — the
  2.1 GiB one, and the one every path loads — cannot be allocated by a caller who forgot to ask.
  The reranker (1.1 GiB) is still guarded at its single lazy getter in `retrieve`, because wrapping
  a cross-encoder in a class to gain a constructor would be structure bought for one call site; BM25
  is 88 KB and deliberately unguarded. The image is built from the repository root so the shared
  module is actually present — and its `.dockerignore` is an allowlist that must never re-include a
  parent directory to help the walker descend: Docker re-tests patterns against parent prefixes,
  `!` patterns included, so `!rag` pulled 5.97 GB back into a context meant to hold 19 files. It **fails open** on anything it cannot
  measure — an engine grounded because `vm_stat` would not parse is a worse outage than the crash it
  prevents — and `EVENTHOUND_ALLOW_LOW_MEMORY=1` is the operator's override. The engine stays **optional**:
  GUI and CLI are 100% functional without it (the three surfaces are independent, §1).
- **The 14b default was not supported by our own corpus — CLOSED 2026-08-29.** The RAM rule above was a
  *hardware* argument and settled nothing about quality. The quality argument had been sitting unactioned
  since 2026-07-24: `docs/analysis/performance.md` measured 7b-instruct at 54/60 cases against the 14b's
  44/60, zero non-Latin-script answers against 7 of 60 (the 14b answered English prompts in Thai —
  coherent, grounded, unreadable), and 2.2x lower latency, with the 14b ahead on narration alone by one
  event against three out of 42. `default_model()` now returns 7b-instruct on every host and the RAM branch
  is gone, because a large machine deserves the better model rather than the heavier one. The 14b is
  selectable and the choice persists (§14.3). Deliberately **not** decided as a side effect of the crash
  fix a day earlier: a memory incident is not evidence about model quality, and letting it stand in for
  one would have reached the right answer for a reason that does not hold.
- **Prompt injection from analyzed data.** Log/EVTX content is untrusted input that enters the context; the boundary is the one `tools/check-injection.sh` guards on ingest. The system prompt must treat packed context as *data to analyze, not instructions to follow*; the read-only tool registry is the structural backstop (even a hijacked model can't act).
- **Volume Shadow Copies / detection via custom-map** (e.g. CurveBall) — niche; rule-based detection is already covered by Hayabusa+Sigma.
